{
  "name": "hisabkhata-root",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "server": "npm run server --prefix backend",
    "server:dev": "npm run server:dev --prefix backend",
    "backend:dev": "npm run dev --prefix backend",
    "backend:deploy": "npm run deploy --prefix backend",
    "install:all": "npm install --prefix frontend && npm install --prefix backend && npm install --prefix functions"
  }
}
